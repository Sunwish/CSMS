<Window x:Class="CommoditySalesManagementSystem.SalesRecordsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommoditySalesManagementSystem"
        mc:Ignorable="d"
        Title="SalesRecordsWindow" Height="295.2" Width="396.8">
    <Grid>
        <ListView x:Name="listView" HorizontalAlignment="Left" Height="246" VerticalAlignment="Top" Width="370" Margin="10,10,0,0" IsSynchronizedWithCurrentItem="True">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Id}" Width="50"></GridViewColumn>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="100"></GridViewColumn>
                    <GridViewColumn Header="Count" DisplayMemberBinding="{Binding Count}" Width="100"></GridViewColumn>
                    <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Money}" Width="100"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
